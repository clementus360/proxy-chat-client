import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["ui-avatars.com","api.dicebear.com", "randomuser.me", "firebasestorage.googleapis.com"],
  },
};

export default nextConfig;
